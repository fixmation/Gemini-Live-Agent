/***** Tailwind v4 uses a zero-config content pipeline, but we keep this file
for future customization if needed. *****/

module.exports = {
  content: ["./index.html", "./src/**/*.{js,jsx,ts,tsx}"]
};
